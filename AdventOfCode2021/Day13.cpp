#include <bits/stdc++.h>

using namespace std;

int main()
{
  freopen("input.txt","r",stdin);
  int currValue,currValue1;
  vector<pair<int,int>> elements;
  char charr;
  while(!cin.eof()){
    cin>>currValue>>charr>>currValue1;
    elements.push_back(make_pair(currValue,currValue1));
  }
}
